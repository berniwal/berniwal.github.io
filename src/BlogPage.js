// src/BlogPage.js — no longer used as a standalone section after the redesign.
// The writing list now lives directly in MainPage.js. Kept as a stub so any
// stale imports don't break.
import React from 'react';

export default function BlogPage() {
  return null;
}
