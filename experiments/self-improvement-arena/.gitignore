# Python build / editable-install artifacts
*.egg-info/
__pycache__/
*.pyc
.pytest_cache/

# Raw per-run JSON logs (the committed artifacts are the PNGs, results.md, replay.json)
results/**/logs/
# Worker / dev-run logs
*.log
